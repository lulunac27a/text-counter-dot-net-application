# Text Counter Application in F# using Fable

This is a text counter application written in F# using Fable.

### Getting Started

Run `npm install` to install dependencies and then run `npm start` to start the development web server.
